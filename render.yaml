services:
  - type: web
    name: receipt-ocr-app
    runtime: docker
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_DEBUG
        value: "false"
      - key: OCR_API_KEY
        sync: false
